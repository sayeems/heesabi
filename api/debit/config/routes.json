{
  "routes": [
    {
      "method": "GET",
      "path": "/debits",
      "handler": "debit.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/debits/count",
      "handler": "debit.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/debits/:id",
      "handler": "debit.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/debits",
      "handler": "debit.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/debits/:id",
      "handler": "debit.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/debits/:id",
      "handler": "debit.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
